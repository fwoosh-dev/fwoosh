{
  "name": "fwoosh-monorepo",
  "private": true,
  "description": "A lightening quick MDX static website generator!",
  "author": "Andrew Lisowski <lisowski54@gmail.com>",
  "workspaces": [
    "packages/*",
    "plugins/*",
    "examples/*"
  ],
  "scripts": {
    "clean": "rimraf '**/tsconfig.tsbuildinfo' && rimraf '**/dist' && rimraf '**/node_modules'",
    "build": "tsc -b tsconfig.dev.json -v && lerna run build",
    "start": "tsc -b tsconfig.dev.json -v --watch",
    "release": "auto shipit"
  },
  "devDependencies": {
    "@types/node": "^14.14.31",
    "auto": "^10.37.6",
    "lerna": "^6.1.0",
    "patch-package": "^6.4.7",
    "prettier": "^2.2.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "@mdx-js/rollup": "^2.2.1",
    "fwoosh": "link:./packages/cli"
  },
  "auto": {
    "plugins": [
      "npm",
      "released"
    ]
  },
  "packageManager": "yarn@3.2.0"
}
